import React from 'react';
import { View, StyleSheet } from 'react-native';
import Svg, { Path, Circle, G } from 'react-native-svg';
import { Colors } from '../../theme';

export interface MapMarker {
  latitude: number;
  longitude: number;
  label: string;
  active: boolean;
}

interface Props {
  markers?: MapMarker[];
  height?: number;
}

// ─── Projection ───────────────────────────────────────────────────────────────
// Simple equirectangular: viewBox 0 0 1000 500
const W = 1000;
const H = 500;

function project(lon: number, lat: number): [number, number] {
  const x = ((lon + 180) / 360) * W;
  const y = ((90 - lat) / 180) * H;
  return [x, y];
}

function toPath(points: [number, number][]): string {
  return points
    .map((p, i) => {
      const [x, y] = project(p[0], p[1]);
      return `${i === 0 ? 'M' : 'L'} ${x.toFixed(1)},${y.toFixed(1)}`;
    })
    .join(' ') + ' Z';
}

// ─── Simplified continent outlines [lon, lat] ─────────────────────────────────

const NORTH_AMERICA: [number, number][] = [
  [-168,72],[-152,72],[-135,70],[-115,75],[-95,74],[-80,67],[-64,63],
  [-55,58],[-66,48],[-68,44],[-75,35],[-65,30],[-58,20],[-60,15],
  [-60,7],[-75,8],[-85,10],[-95,25],[-117,32],[-124,46],[-126,50],[-137,58],
];

const SOUTH_AMERICA: [number, number][] = [
  [-82,8],[-78,0],[-60,-5],[-50,-15],[-44,-23],[-52,-33],[-62,-38],
  [-68,-45],[-70,-50],[-65,-55],[-58,-50],[-63,-42],[-67,-35],[-65,-25],
  [-70,-15],[-75,-5],[-77,2],
];

const EUROPE: [number, number][] = [
  [-10,36],[0,36],[5,43],[10,44],[15,44],[18,40],[22,38],[28,36],
  [34,37],[36,41],[40,42],[44,43],[40,48],[35,48],[32,52],[28,56],
  [24,60],[20,62],[15,58],[10,55],[5,52],[0,51],[-5,48],[-8,44],[-10,36],
];

const AFRICA: [number, number][] = [
  [-17,14],[-15,10],[-15,5],[-5,5],[0,5],[5,4],[10,4],[15,4],
  [20,3],[25,3],[30,5],[34,8],[42,12],[44,12],[44,0],[40,-10],
  [35,-18],[32,-25],[28,-32],[18,-35],[15,-30],[12,-20],[10,-5],
  [8,5],[5,5],[2,10],[0,14],[-5,12],[-10,10],[-17,14],
];

const ASIA: [number, number][] = [
  [34,37],[36,41],[40,42],[44,43],[48,40],[55,38],[60,35],[65,35],
  [70,35],[75,32],[80,28],[85,22],[90,22],[95,20],[100,12],[105,10],
  [110,5],[118,-8],[125,-15],[135,-22],[140,-30],[145,-38],[150,-38],
  [155,-28],[160,-22],[168,-8],[175,0],[180,10],[175,20],[170,35],
  [175,60],[170,65],[165,68],[160,68],[155,60],[145,55],[140,50],
  [135,45],[130,40],[125,35],[120,30],[115,25],[110,18],[105,15],
  [100,10],[95,8],[90,12],[85,20],[80,28],[75,35],[70,40],[65,45],
  [60,50],[55,55],[50,60],[45,55],[40,50],[36,46],[32,48],[28,56],
  [24,60],[20,62],[25,62],[30,65],[38,68],[45,68],[50,70],[55,72],
  [60,70],[65,68],[70,68],[75,70],[80,73],[85,70],[90,72],[95,68],
  [100,65],[105,62],[110,55],[115,45],[120,38],[125,32],[120,25],
  [105,20],[100,12],[95,8],[90,12],[85,20],[80,28],[75,35],[70,40],
  [65,45],[60,50],[55,55],[50,60],[45,55],[40,50],[36,46],[32,52],
  [28,56],[24,60],[20,62],[15,58],[10,55],[5,52],[0,51],[-5,48],
  [-8,44],[-10,36],[0,36],[5,43],[10,44],[15,44],[18,40],[22,38],
  [28,36],[34,37],
];

const AUSTRALIA: [number, number][] = [
  [114,-22],[116,-20],[120,-18],[125,-14],[130,-12],[136,-12],[138,-14],
  [140,-18],[142,-22],[145,-25],[148,-30],[152,-30],[154,-28],[152,-24],
  [150,-22],[153,-26],[152,-30],[148,-38],[144,-38],[140,-36],[136,-35],
  [130,-32],[124,-34],[118,-34],[114,-30],[112,-25],[114,-22],
];

const GREENLAND: [number, number][] = [
  [-48,84],[-30,82],[-18,78],[-22,74],[-30,72],[-44,70],[-52,68],
  [-58,68],[-62,72],[-68,76],[-65,80],[-52,84],[-48,84],
];

const ANTARCTICA: [number, number][] = [
  [-180,-72],[-150,-68],[-120,-68],[-90,-72],[-60,-70],[-30,-72],
  [0,-70],[30,-72],[60,-70],[90,-72],[120,-68],[150,-68],[180,-72],
  [180,-90],[-180,-90],
];

const LANDMASSES = [
  NORTH_AMERICA,
  SOUTH_AMERICA,
  EUROPE,
  AFRICA,
  ASIA,
  AUSTRALIA,
  GREENLAND,
  ANTARCTICA,
];

// ─── Component ────────────────────────────────────────────────────────────────

export default function WorldMap({ markers = [], height = 200 }: Props) {
  return (
    <View style={[styles.container, { height }]}>
      <Svg
        viewBox={`0 0 ${W} ${H}`}
        width="100%"
        height="100%"
        preserveAspectRatio="xMidYMid meet"
      >
        {/* Ocean background */}
        <Path d={`M0,0 L${W},0 L${W},${H} L0,${H} Z`} fill="#0a0a0a" />

        {/* Landmasses */}
        <G>
          {LANDMASSES.map((points, i) => (
            <Path
              key={i}
              d={toPath(points)}
              fill="#2a2a2a"
              stroke="#1a1a1a"
              strokeWidth={1}
            />
          ))}
        </G>

        {/* Datacenter markers */}
        {markers.map((m, i) => {
          const [x, y] = project(m.longitude, m.latitude);
          return (
            <G key={i}>
              {m.active && (
                <Circle
                  cx={x}
                  cy={y}
                  r={8}
                  fill={Colors.primary}
                  opacity={0.15}
                />
              )}
              <Circle
                cx={x}
                cy={y}
                r={4}
                fill={m.active ? Colors.primary : '#555'}
                stroke={m.active ? Colors.primary : '#777'}
                strokeWidth={1}
              />
            </G>
          );
        })}
      </Svg>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#0a0a0a',
    borderRadius: 16,
    overflow: 'hidden',
  },
});
